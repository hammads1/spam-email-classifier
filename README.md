# spam-email-classifier
Spam Email Classifier is a Python machine learning project that detects whether an email is spam or not. It uses TF-IDF vectorization to convert email text into numerical features and a Naive Bayes classifier to make predictions. The project demonstrates text preprocessing, model training, evaluation, and prediction on new unseen emails.
